Binary files KlipperLCD/.git/index and KlipperLCD-patched/.git/index differ
diff -uNr KlipperLCD/.git/logs/HEAD KlipperLCD-patched/.git/logs/HEAD
--- KlipperLCD/.git/logs/HEAD	2025-09-06 23:58:05.221996217 +0300
+++ KlipperLCD-patched/.git/logs/HEAD	2025-09-07 02:12:32.106957406 +0300
@@ -1 +1 @@
-0000000000000000000000000000000000000000 25b2f6c8295d4df9cb274a32f236e349640f6aee SweetCapitan <danil.duhanin@yandex.ru> 1757192285 +0300	clone: from https://github.com/joakimtoe/KlipperLCD.git
+0000000000000000000000000000000000000000 25b2f6c8295d4df9cb274a32f236e349640f6aee SweetCapitan <danil.duhanin@yandex.ru> 1757200352 +0300	clone: from https://github.com/joakimtoe/KlipperLCD.git
diff -uNr KlipperLCD/.git/logs/refs/heads/main KlipperLCD-patched/.git/logs/refs/heads/main
--- KlipperLCD/.git/logs/refs/heads/main	2025-09-06 23:58:05.221996217 +0300
+++ KlipperLCD-patched/.git/logs/refs/heads/main	2025-09-07 02:12:32.106957406 +0300
@@ -1 +1 @@
-0000000000000000000000000000000000000000 25b2f6c8295d4df9cb274a32f236e349640f6aee SweetCapitan <danil.duhanin@yandex.ru> 1757192285 +0300	clone: from https://github.com/joakimtoe/KlipperLCD.git
+0000000000000000000000000000000000000000 25b2f6c8295d4df9cb274a32f236e349640f6aee SweetCapitan <danil.duhanin@yandex.ru> 1757200352 +0300	clone: from https://github.com/joakimtoe/KlipperLCD.git
diff -uNr KlipperLCD/.git/logs/refs/remotes/origin/HEAD KlipperLCD-patched/.git/logs/refs/remotes/origin/HEAD
--- KlipperLCD/.git/logs/refs/remotes/origin/HEAD	2025-09-06 23:58:05.219996317 +0300
+++ KlipperLCD-patched/.git/logs/refs/remotes/origin/HEAD	2025-09-07 02:12:32.105957462 +0300
@@ -1 +1 @@
-0000000000000000000000000000000000000000 25b2f6c8295d4df9cb274a32f236e349640f6aee SweetCapitan <danil.duhanin@yandex.ru> 1757192285 +0300	clone: from https://github.com/joakimtoe/KlipperLCD.git
+0000000000000000000000000000000000000000 25b2f6c8295d4df9cb274a32f236e349640f6aee SweetCapitan <danil.duhanin@yandex.ru> 1757200352 +0300	clone: from https://github.com/joakimtoe/KlipperLCD.git
diff -uNr KlipperLCD/klipper_lcd_.patch KlipperLCD-patched/klipper_lcd_.patch
--- KlipperLCD/klipper_lcd_.patch	2025-09-07 01:05:38.182163599 +0300
+++ KlipperLCD-patched/klipper_lcd_.patch	1970-01-01 03:00:00.000000000 +0300
@@ -1,36 +0,0 @@
-diff --git a/main.py b/main.py
-index d3f4bab..94c017d 100755
---- a/main.py
-+++ b/main.py
-@@ -4,15 +4,22 @@ import time
- import base64
- from threading import Thread
- from datetime import timedelta
-+import io
-+import os
- 
- from printer import PrinterData
- from lcd import LCD, _printerData
- 
-+lcd_device = os.getenv('LCD_DEVICE', '/dev/ttyAMA0')
-+klippy_sock = os.getenv('KLIPPY_SOCK', '/home/pi/printer_data/comms/klippy.sock')
-+moonraker_url = os.getenv('MOONRAKER_URL', '127.0.0.1')
-+moonraker_key = os.getenv('MOONRAKER_KEY', 'XXXXXX')
-+
- class KlipperLCD ():
-     def __init__(self):
--        self.lcd = LCD("/dev/ttyAMA0", callback=self.lcd_callback)
-+        self.lcd = LCD(lcd_device, callback=self.lcd_callback)
-         self.lcd.start()
--        self.printer = PrinterData('XXXXXX', URL=("127.0.0.1"), klippy_sock='/home/pi/printer_data/comms/klippy.sock', callback=self.printer_callback)
-+        self.printer = PrinterData(moonraker_key, URL=moonraker_url, klippy_sock=klippy_sock, callback=self.printer_callback)
-         self.running = False
-         self.wait_probe = False
-         self.thumbnail_inprogress = False
-@@ -222,4 +229,4 @@ class KlipperLCD ():
- 
- if __name__ == "__main__":
-     x = KlipperLCD()
--    x.start()
-\ No newline at end of file
-+    x.start()
diff -uNr KlipperLCD/klipper_lcd_patch.0001 KlipperLCD-patched/klipper_lcd_patch.0001
--- KlipperLCD/klipper_lcd_patch.0001	2025-09-07 00:16:37.645946938 +0300
+++ KlipperLCD-patched/klipper_lcd_patch.0001	1970-01-01 03:00:00.000000000 +0300
@@ -1,35 +0,0 @@
-diff --git a/main.py b/main.py
-index d3f4bab..2323491 100755
---- a/main.py
-+++ b/main.py
-@@ -4,15 +4,21 @@ import time
- import base64
- from threading import Thread
- from datetime import timedelta
-+import io
- 
- from printer import PrinterData
- from lcd import LCD, _printerData
- 
-+lcd_device = os.getenv('LCD_DEVICE', '/dev/ttyAMA0')
-+klippy_sock = os.getenv('KLIPPY_SOCK', '/home/pi/printer_data/comms/klippy.sock')
-+moonraker_url = os.getenv('MOONRAKER_URL', '127.0.0.1')
-+moonraker_key = os.getenv('MOONRAKER_KEY', 'XXXXXX')
-+
- class KlipperLCD ():
-     def __init__(self):
--        self.lcd = LCD("/dev/ttyAMA0", callback=self.lcd_callback)
-+        self.lcd = LCD(lcd_device, callback=self.lcd_callback)
-         self.lcd.start()
--        self.printer = PrinterData('XXXXXX', URL=("127.0.0.1"), klippy_sock='/home/pi/printer_data/comms/klippy.sock', callback=self.printer_callback)
-+        self.printer = PrinterData(moonraker_key, URL=moonraker_url, klippy_sock=klippy_sock, callback=self.printer_callback)
-         self.running = False
-         self.wait_probe = False
-         self.thumbnail_inprogress = False
-@@ -222,4 +228,4 @@ class KlipperLCD ():
- 
- if __name__ == "__main__":
-     x = KlipperLCD()
--    x.start()
-\ No newline at end of file
-+    x.start()
diff -uNr KlipperLCD/main.py KlipperLCD-patched/main.py
--- KlipperLCD/main.py	2025-09-07 01:05:29.606673340 +0300
+++ KlipperLCD-patched/main.py	2025-09-07 02:12:32.276947930 +0300
@@ -4,22 +4,15 @@
 import base64
 from threading import Thread
 from datetime import timedelta
-import io
-import os
 
 from printer import PrinterData
 from lcd import LCD, _printerData
 
-lcd_device = os.getenv('LCD_DEVICE', '/dev/ttyAMA0')
-klippy_sock = os.getenv('KLIPPY_SOCK', '/home/pi/printer_data/comms/klippy.sock')
-moonraker_url = os.getenv('MOONRAKER_URL', '127.0.0.1')
-moonraker_key = os.getenv('MOONRAKER_KEY', 'XXXXXX')
-
 class KlipperLCD ():
     def __init__(self):
-        self.lcd = LCD(lcd_device, callback=self.lcd_callback)
+        self.lcd = LCD("/dev/ttyAMA0", callback=self.lcd_callback)
         self.lcd.start()
-        self.printer = PrinterData(moonraker_key, URL=moonraker_url, klippy_sock=klippy_sock, callback=self.printer_callback)
+        self.printer = PrinterData('XXXXXX', URL=("127.0.0.1"), klippy_sock='/home/pi/printer_data/comms/klippy.sock', callback=self.printer_callback)
         self.running = False
         self.wait_probe = False
         self.thumbnail_inprogress = False
@@ -229,4 +222,4 @@
 
 if __name__ == "__main__":
     x = KlipperLCD()
-    x.start()
+    x.start()
\ No newline at end of file
diff -uNr KlipperLCD/p.patch KlipperLCD-patched/p.patch
--- KlipperLCD/p.patch	2025-09-07 01:59:35.542845596 +0300
+++ KlipperLCD-patched/p.patch	1970-01-01 03:00:00.000000000 +0300
@@ -1,60 +0,0 @@
-diff --git a/main.py b/main.py
-index d3f4bab..94c017d 100755
---- a/main.py
-+++ b/main.py
-@@ -4,15 +4,22 @@ import time
- import base64
- from threading import Thread
- from datetime import timedelta
-+import io
-+import os
- 
- from printer import PrinterData
- from lcd import LCD, _printerData
- 
-+lcd_device = os.getenv('LCD_DEVICE', '/dev/ttyAMA0')
-+klippy_sock = os.getenv('KLIPPY_SOCK', '/home/pi/printer_data/comms/klippy.sock')
-+moonraker_url = os.getenv('MOONRAKER_URL', '127.0.0.1')
-+moonraker_key = os.getenv('MOONRAKER_KEY', 'XXXXXX')
-+
- class KlipperLCD ():
-     def __init__(self):
--        self.lcd = LCD("/dev/ttyAMA0", callback=self.lcd_callback)
-+        self.lcd = LCD(lcd_device, callback=self.lcd_callback)
-         self.lcd.start()
--        self.printer = PrinterData('XXXXXX', URL=("127.0.0.1"), klippy_sock='/home/pi/printer_data/comms/klippy.sock', callback=self.printer_callback)
-+        self.printer = PrinterData(moonraker_key, URL=moonraker_url, klippy_sock=klippy_sock, callback=self.printer_callback)
-         self.running = False
-         self.wait_probe = False
-         self.thumbnail_inprogress = False
-@@ -222,4 +229,4 @@ class KlipperLCD ():
- 
- if __name__ == "__main__":
-     x = KlipperLCD()
--    x.start()
-\ No newline at end of file
-+    x.start()
-diff --git a/printer.py b/printer.py
-index cfbf0bb..7909448 100644
---- a/printer.py
-+++ b/printer.py
-@@ -419,9 +419,9 @@ class PrinterData:
- 		self.update_variable()
- 
- 		#alternative approach
--		#full_version = self.getREST('/printer/info')['result']['software_version']
--		#self.SHORT_BUILD_VERSION = '-'.join(full_version.split('-',2)[:2])
--		self.SHORT_BUILD_VERSION = self.getREST('/machine/update/status?refresh=false')['result']['version_info']['klipper']['version']
-+		full_version = self.getREST('/printer/info')['result']['software_version']
-+		self.SHORT_BUILD_VERSION = '-'.join(full_version.split('-',2)[:2])
-+		#self.SHORT_BUILD_VERSION = self.getREST('/machine/update/status?refresh=false')['result']['version_info']['klipper']['version']
- 
- 		data = self.getREST('/printer/objects/query?toolhead')['result']['status']
- 		#print(json.dumps(data, indent=2))
-@@ -668,4 +668,4 @@ class PrinterData:
- 		self.setExtTemp(exttemp)
- 
- 	def setZOffset(self, offset):
--		self.sendGCode('SET_GCODE_OFFSET Z=%s MOVE=1' % offset)
-\ No newline at end of file
-+		self.sendGCode('SET_GCODE_OFFSET Z=%s MOVE=1' % offset)
diff -uNr KlipperLCD/printer.py KlipperLCD-patched/printer.py
--- KlipperLCD/printer.py	2025-09-07 01:59:21.080608875 +0300
+++ KlipperLCD-patched/printer.py	2025-09-07 02:12:32.276947930 +0300
@@ -419,9 +419,9 @@
 		self.update_variable()
 
 		#alternative approach
-		full_version = self.getREST('/printer/info')['result']['software_version']
-		self.SHORT_BUILD_VERSION = '-'.join(full_version.split('-',2)[:2])
-		#self.SHORT_BUILD_VERSION = self.getREST('/machine/update/status?refresh=false')['result']['version_info']['klipper']['version']
+		#full_version = self.getREST('/printer/info')['result']['software_version']
+		#self.SHORT_BUILD_VERSION = '-'.join(full_version.split('-',2)[:2])
+		self.SHORT_BUILD_VERSION = self.getREST('/machine/update/status?refresh=false')['result']['version_info']['klipper']['version']
 
 		data = self.getREST('/printer/objects/query?toolhead')['result']['status']
 		#print(json.dumps(data, indent=2))
@@ -668,4 +668,4 @@
 		self.setExtTemp(exttemp)
 
 	def setZOffset(self, offset):
-		self.sendGCode('SET_GCODE_OFFSET Z=%s MOVE=1' % offset)
+		self.sendGCode('SET_GCODE_OFFSET Z=%s MOVE=1' % offset)
\ No newline at end of file
